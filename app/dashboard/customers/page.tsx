import { Metadata } from 'next';
import React from 'react';
 
export const metadata: Metadata = {
  title: 'Customers',
  description: 'The customers page.',
};

export default function Page() {
    return <p>Customers Page</p>;
  }
